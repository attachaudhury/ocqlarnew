

<!--  
<form method="post"action="/admin/category/update/:id">

    <table>

        <tr>
            <td>Name : </td>
            <td>
                <input type="text" placeholder="Name" name="name" value="<%= category.name %>" required> </td>
        </tr>
        <tr>
            <td> Keywords :</td>
            <td>
                <input type="text" placeholder="Keywords" name="cat_keywords" value="<%= category.cat_keywords %>" required>
            </td>
        </tr>
        <tr>
            <td>Category_Type :</td>
            <td>
                <input type="text" placeholder="Category_Type" name="cat_typ" value="<%= category.cat_type %>" required>
            </td>
        </tr>
        <tr>
            <td>Featured_type :</td>
            <td>
                <input type="text" placeholder="Featured_type" name="featured_type" value="<%= category.featured_type %>" required>
            </td>
        </tr>

        <tr>

            <td>
                <button type="submit" class="btn btn-info"><i class="fa fa-database"></i> Submit</button>
                  <!-- <input type="button" value="Display" onclick="window.location='/category/list';"> -->
            <!-- </td>
        </tr>
    </table>





</form> --> 

<% layout('./layout') -%>



<form action="/admin/category/update"autocomplete="off" method="POST">
    <input type="hidden" name="_id" value="<%=category._id%>">
    <div class="form-group">
        <label>Name</label>
        <input type="text" class="form-control" name="name" placeholder="Name"value="<%= category.name %>" >
        <!-- <div class="text-danger">
            {{category.fullNameError}}</div> -->
    </div>
    <div class="form-group">
        <label>Keywords</label>
        <input type="text" class="form-control" name="cat_keywords" placeholder="Keywords"value="<%= category.cat_keywords %>">
        <!-- <div class="text-danger">
            {{category.emailError}}</div>
    </div> -->

  
    <div class="form-row">
        <div class="form-group col-md-6">
            <label>category_type</label>
            <select class="form-control" name="cat_type" tabindex="-1" aria-hidden="true">
                <option value="static">Static</option>
                <option value="animated">Animated</option>
            </select>        
        </div>
        <div class="form-group col-md-6">
            <label>Featured_type</label>
            <select class="form-control" name="featured_type" tabindex="-1" aria-hidden="true">
                <option value="static">Static</option>
                <option value="animated">Animated</option>
            </select>        
        </div>
    </div>
    <div class="form-group">
        <button type="submit" class="btn btn-info"><i class="fa fa-database"></i> Submit</button>
    </div>
</form>